import { StyleSheet } from 'react-native';
import appTheme from '@/theme';

const styles = StyleSheet.create({
  container: {
    backgroundColor: appTheme.lightDark,
    paddingVertical: appTheme.windowSize * 0.01,
    paddingLeft: 20,
  },
});

export default styles;
